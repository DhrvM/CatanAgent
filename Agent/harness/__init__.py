"""
Agent harness (Trading + Development): mock server + ``HarnessOpenAI`` for scripted
``[thought]`` / ``[action]`` lines under ``--mock``.

Run: ``python -m Agent.harness.lab`` from the repo root (with venv activated).
"""
