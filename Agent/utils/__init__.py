# Agent/utils — client and processor utilities
#
# Import individual modules directly rather than pulling everything
# into the package namespace, to avoid forcing heavy deps at import time.
#
# Usage:
#   from Agent.utils.socket_client import CatanSocketClient
#   from Agent.utils.openai_client import OpenAIClient
